#include "main.h"

/**
 * signal_intercepter - Intercepts a signal
 *
 * @sig: signal number being intercepted
 *
 * Description:
 * Function used to handle a signal and intercept it
 *
 * Return: Always 0 (Success)
 */

void signal_intercepter(int sig)
{
	(void)sig;
}
